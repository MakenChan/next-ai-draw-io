# 数据流图（DFD）
- 优先表达 Context、0 层以及必要的 1 层；信息不足时不要强行补层。
- Context 只保留总过程 P0、外部实体与已命名数据流。
- 0 层把 P0 分解为 1..n 号过程，并接入外部实体和 D1..Dn 数据存储。
- 1 层只分解一个父过程，父子图输入/输出保持平衡。
- 数据存储命名为 D编号 + 名称，跨层复用时编号和语义不变。
- 所有流为正交折线，具备源、目标、方向和语义标签。
- 双向流拆成两条独立 edge 并走不同通道。
- 不混入 UML Actor、类框或流程判断菱形。
