# 业务流程图
- 开始/结束用胶囊终止符，处理用矩形，判断用菱形；判断至少两条带语义的分支。
- 失败、重试或返回路径必须显式回环；主干自上而下，分支向两侧展开。
- 判断节点所有出边分别标注“是/否”“成功/失败”等语义。
- 先为主干和分支节点分配区域，再为回环线预留独立通道；回环 edge 在同一个 mxGeometry 中写显式 points。
- 同侧多条回环按跨度逐级外移，避免共用通道。
- 任意节点不得重叠；连线不得穿过无关节点；标签不得压线。
