# 顺序图
- 人物/角色顶部用 Actor；页面、服务、数据库等用矩形头。
- 生命线为单根竖直虚线；激活条为白色窄矩形并遮住生命线。
- 每条消息独占 y 通道并保持水平，相邻消息错开 36~56；多跳调用逐条下移。
- 消息连接生命线或当前激活条，不连接顶部标题框。
- 调用为实线实心箭头，返回为虚线箭头。
- 同步调用的接收方激活条从调用到达处开始或更早，持续到对应返回之后；调用者等待下游返回期间保持激活。
- 嵌套调用按调用栈逐层闭合，最内层先结束。
- 除非用户要求代码级时序，不直接展示类后缀、HTTP 路径或方法签名。
